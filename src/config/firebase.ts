// Firebase has been removed from the CaseFlow AI runtime.
// CaseFlow AI now communicates with the Python/FastAPI backend.
//
// Kept temporarily as an empty compatibility module because
// some legacy components still import from this file.

export const db = null;